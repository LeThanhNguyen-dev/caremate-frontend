import { useEffect, useState, useCallback } from 'react';
import { useToast } from '../hooks/useToast';
import caremateApi from '../api/caremateApi';
import type { ServiceDetailDto } from '../api/frontend-api-contract'; const AdminSettings = () => { const { showToast } = useToast(); const [services, setServices] = useState<ServiceDetailDto[]>([]); const [editingId, setEditingId] = useState<number | null>(null); const [form, setForm] = useState({ name: '', description: '', basePrice: '', estimatedDurationMinutes: '', status: 'active' }); const [loading, setLoading] = useState(true); const load = useCallback(async () => { try { setLoading(true); const data = await caremateApi.getServices(); setServices(data); } catch { showToast('Không thể tải danh sách.', 'error'); } finally { setLoading(false); } }, [showToast]); useEffect(() => { void load(); }, [load]); const startEdit = async (id: number) => { try { const s = await caremateApi.getServiceById(id); setEditingId(id); setForm({ name: s.name, description: s.description || '', basePrice: String(s.basePrice), estimatedDurationMinutes: String(s.estimatedDurationMinutes), status: s.status }); } catch { showToast('Không thể tải dịch vụ.', 'error'); } }; const save = async (e: React.FormEvent) => { e.preventDefault(); if (!editingId) return; try { await caremateApi.updateService(editingId, { name: form.name, description: form.description, basePrice: Number(form.basePrice), estimatedDurationMinutes: Number(form.estimatedDurationMinutes), status: form.status }); setEditingId(null); showToast('Cập nhật dịch vụ thành công!', 'success'); await load(); } catch { showToast('Cập nhật thất bại.', 'error'); } }; if (loading) return ( <div className="flex items-center justify-center min-h-[400px]"> <div className="flex flex-col items-center gap-3"><div className="spinner"></div><span className="text-sm text-slate-500">Đang tải...</span></div> </div> ); return ( <div className="p-6 lg:p-8 max-w-4xl"> <div className="mb-8"> <h1 className="text-2xl font-bold text-slate-900">Cài đặt hệ thống</h1> <p className="text-sm text-slate-500 mt-1">Quản lý danh mục dịch vụ v� cấu hình hệ thống</p> </div> <div className="space-y-3"> {services.map((s) => ( <div key={s.id} className="card px-6 py-4 flex flex-col sm:flex-row sm:items-center justify-between gap-3"> <div> <h3 className="font-semibold text-slate-900">{s.name}</h3> <div className="flex items-center gap-3 text-sm text-slate-500 mt-1"> <span>{s.basePrice.toLocaleString()} VNĐ</span> <span>·</span> <span>{s.estimatedDurationMinutes} phút</span> <span>·</span> {s.status === 'active' ? <span className="badge bg-green-50 text-green-600 font-bold px-3 py-1 rounded-full text-[10px] uppercase tracking-widest text-[10px]">Hoạt Động</span> : <span className="badge bg-red-50 text-red-600 font-bold px-3 py-1 rounded-full text-[10px] uppercase tracking-widest text-[10px]">Ngừng</span>} </div> </div> <button className="btn-secondary btn-sm text-xs" onClick={() => void startEdit(s.id)}>Chỉnh sửa</button> </div> ))} </div> {editingId && ( <form onSubmit={save} className="card card-body mt-6"> <div className="flex items-center justify-between mb-4"> <h2 className="text-lg font-semibold text-slate-900">Chỉnh sửa dịch vụ #{editingId}</h2> <button type="button" onClick={() => setEditingId(null)} className="text-slate-400 hover:text-slate-600"> <svg className="w-5 h-5" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><path d="M6 18L18 6M6 6l12 12" /></svg> </button> </div> <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-4"> <div> <label className="form-label">Tên dịch vụ</label> <input className="form-input" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} required /> </div> <div> <label className="form-label">Trạng thái</label> <select className="form-select" value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value })}> <option value="active">Hoạt động</option> <option value="inactive">Ngừng</option> </select> </div> <div> <label className="form-label">Đơn giá (VNĐ)</label> <input type="number" className="form-input" value={form.basePrice} onChange={(e) => setForm({ ...form, basePrice: e.target.value })} required /> </div> <div> <label className="form-label">Thời gian (Phút)</label> <input type="number" className="form-input" value={form.estimatedDurationMinutes} onChange={(e) => setForm({ ...form, estimatedDurationMinutes: e.target.value })} required /> </div> <div className="sm:col-span-2"> <label className="form-label">Mô tả</label> <textarea className="form-textarea" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} rows={3} /> </div> </div> <div className="flex gap-3"> <button type="submit" className="btn-primary">Lưu thay đổi</button> <button type="button" className="btn-secondary" onClick={() => setEditingId(null)}>Hủy</button> </div> </form> )} </div> );
}; export default AdminSettings; 
