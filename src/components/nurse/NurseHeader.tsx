import { Link, useLocation } from 'react-router-dom';
import { useAuth } from '../../hooks/useAuth'; const pageMeta: Record<string, { title: string; subtitle: string }> = { '/nurse/overview': { title: 'Tổng quan vận hành', subtitle: 'Theo dõi lịch hẹn, năng lực phục vụ v� tiến độ hồ sơ trong một màn hình.', }, '/nurse/schedule': { title: 'Lịch làm việc', subtitle: 'Quản lý slot rảnh theo kiểu lịch, không cần nhập thủ công từng mốc giờ.', }, '/nurse/bookings': { title: 'Quản lý lịch hẹn', subtitle: 'Xử lý booking, cập nhật trạng thái v� theo dõi ca sắp diễn ra.', }, '/nurse/services': { title: 'Dịch vụ cung cấp', subtitle: 'Kiểm soát danh mục dịch vụ, mức giá v� trạng thái hoạt động.', }, '/nurse/profile': { title: 'Hồ sơ y tá', subtitle: 'Cập nhật giới thiệu, tài liệu xác minh v� thông tin hành nghề.', },
}; const NurseHeader = () => { const location = useLocation(); const { user } = useAuth(); const meta = pageMeta[location.pathname] ?? { title: 'Không gian y tá', subtitle: 'Không gian làm việc dành cho điều dưỡng CareMate.', }; return ( <header className="sticky top-0 z-40 border-b border-transparent bg-slate-900/[0.01] px-6 py-5 backdrop-blur-3xl lg:px-8"> <div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"> <div> <div className="text-xs font-bold uppercase tracking-[0.24em] text-slate-400">Không gian y tá</div> <h1 className="mt-2 font-heading text-3xl font-extrabold text-slate-900">{meta.title}</h1> <p className="mt-2 max-w-3xl text-sm font-medium leading-6 text-slate-500">{meta.subtitle}</p> </div> <div className="flex flex-wrap items-center gap-3"> <div className="rounded-2xl bg-slate-900/[0.04] px-5 py-3 shadow-sm"> <div className="text-xs font-bold uppercase tracking-[0.24em] text-slate-400">Tài khoản</div> <div className="mt-1 text-sm font-bold text-slate-900">{user?.username}</div> </div> <Link to="/notifications" className="btn-secondary bg-slate-900/[0.02] text-nurse font-bold active:scale-95 transition-all hover:bg-nurse/10 btn-sm font-bold">Thông báo</Link> <Link to="/nurse/schedule" className="btn-primary active:scale-95 transition-all btn-sm px-6 font-bold">Mở lịch</Link> </div> </div> </header> );
}; export default NurseHeader; 
