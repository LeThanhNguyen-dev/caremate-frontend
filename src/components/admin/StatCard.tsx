import { ArrowUpIcon, ArrowDownIcon } from '@heroicons/react/20/solid'; interface StatCardProps { label: string; value: string; trend?: string; trendUp?: boolean; icon: React.ComponentType<React.SVGProps<SVGSVGElement>>; iconColor: string; iconBg: string; badgeValue?: string; badgeColor?: string; } const StatCard = ({ label, value, trend, trendUp, icon: Icon, iconColor, iconBg, badgeValue }: StatCardProps) => { return ( <div className="bg-white rounded-2xl p-6 shadow-[0_4px_20px_rgba(0,0,0,0.03)] border-transparent -gray-50 hover:shadow-lg transition-shadow duration-300"> <div className="flex justify-between items-start mb-4"> <div className={`p-3 rounded-xl ${iconBg} ${iconColor}`}> <Icon className="w-6 h-6" /> </div> {badgeValue && ( <span className={`px-2.5 py-1 rounded-full text-xs font-bold bg-orange-100 text-orange-600`}> {badgeValue} </span> )} </div> <div> <p className="text-gray-500 text-sm font-medium mb-1">{label}</p> <div className="flex items-end gap-2"> <h3 className="text-2xl font-bold text-gray-900">{value}</h3> {trend && ( <div className={`flex items-center text-xs font-bold mb-1.5 ${trendUp ? 'text-green-500' : 'text-red-500'}`}> {trendUp ? <ArrowUpIcon className="w-3 h-3 mr-0.5" /> : <ArrowDownIcon className="w-3 h-3 mr-0.5" />} {trend} </div> )} </div> </div> </div> ); }; export default StatCard; 
